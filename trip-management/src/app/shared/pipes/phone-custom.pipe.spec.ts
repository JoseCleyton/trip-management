import { PhoneCustomPipe } from './phone-custom.pipe';

describe('PhoneCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
